# Object-Oriented Programming (OOP)

This folder is for CMP2103 / C++ Object-Oriented Programming work.

## Suggested structure

```text
OOP/
├── Credit_Card_Validation/
│   ├── main.cpp
│   ├── validate_credit_card.cppm
│   └── README.md
├── Exercises/
├── Notes/
└── README.md
```

## Current topic

### Credit Card Validation using the Luhn Algorithm

A good task folder can contain:

- `main.cpp` — reads the card number and displays whether it is valid.
- `validate_credit_card.cppm` — contains the validation functions.
- `README.md` — explains the problem, algorithm, compilation, and sample input/output.

More OOP exercises can be added as separate subfolders as the module progresses.
